# Definimos un conjunto llamado 's' usando la sintaxis de llaves {}.
# Al igual que con set(), los duplicados se eliminan y no hay un orden garantizado.
s = {5,4 ,6,8,8,1}

# Imprimimos el conjunto resultante.
print(s)
# Imprimimos el tipo de dato de 's', que es 'set'.
print(type(s))