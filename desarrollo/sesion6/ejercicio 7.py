# Creamos una lista llamada 'set' (aunque es mejor no usar nombres de funciones integradas como 'set').
set = ([1,2,2,3,4])
# Imprimimos la longitud del conjunto 's' (definido en celdas anteriores).
# Nota: Aunque la variable se llama 'set', en la siguiente línea se imprime la longitud de la variable 's' que es un conjunto, no la lista 'set' recién creada. Esto puede generar confusión.
print(len(s))