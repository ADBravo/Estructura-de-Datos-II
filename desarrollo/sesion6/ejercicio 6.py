# Creamos un conjunto llamado 's'.
s = set([5,6,7,8])

# Iteramos sobre cada elemento del conjunto 's'.
for ss in s:
  # Imprimimos cada elemento del conjunto en una línea separada.
  print(ss)